apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: twotakeone
  labels:
    app: backend
    app.kubernetes.io/name: backend
spec:
  type: ClusterIP
  selector: { app: backend }
  ports:
    - port: 8080
      targetPort: 8080
