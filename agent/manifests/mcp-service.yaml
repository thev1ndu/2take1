apiVersion: v1
kind: Service
metadata:
  name: mcp-server
  namespace: twotakeone
  labels:
    app: mcp-server
    app.kubernetes.io/name: mcp-server
spec:
  type: ClusterIP
  selector: { app: mcp-server }
  ports:
    - port: 8081
      targetPort: 8081
