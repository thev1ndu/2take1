apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Every resource declares its own namespace (todo-agent), so the namespace
# transformer is intentionally unused -- same convention as the root
# kustomization.yaml and agent/kustomization.yaml.
#
# Prerequisite (cluster-wide, one-time, not part of this kustomization):
# install KEDA. See README.md.

resources:
  - manifests/namespace.yaml
  # - manifests/redis-deployment.yaml
  # - manifests/redis-service.yaml
  # - manifests/networkpolicy-redis.yaml
  # - manifests/init-agent-stream-job.yaml
  # - manifests/mcp-server-deployment.yaml
  # - manifests/mcp-server-service.yaml
  # - manifests/networkpolicy-mcp-server.yaml
  # - manifests/agent-worker-deployment.yaml
  # - manifests/scaledobject-agent-worker.yaml
  # - manifests/websocket-gateway-deployment.yaml
  # - manifests/websocket-gateway-service.yaml
  # - manifests/web-deployment.yaml
  # - manifests/web-service.yaml
  # - manifests/ingress.yaml
  # secret-anthropic.yaml (copied from secret-anthropic.example.yaml, real
  # key filled in, never committed) must be added here before applying.
